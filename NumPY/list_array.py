data = [50,50,47,97,49,3,53,42]
print(data)
print(data*2)

"""here we can clearly see data repeating again when multiplied but we actually wanted to double it
    this problem is soved by numpy that example is in another module"""